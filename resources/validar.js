// JavaScript Document
function validar(){
    if (client.T1.value.length==0){
        alert("Ingresa nombre de usuario")
        client.T1.focus()
        return (false)
    }
	/* if (loginfrm.password.value.length==0){
        alert("Ingresa clave de accseso")
        loginfrm.password.focus()
        return (false)
    }*/
}